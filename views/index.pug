extends layout

block content
  h1 Welcome to La Librairie

  table
    thead
      tr
        th Title
        th Author
        th Description
    tbody
      for book in books
        tr
          td
            a(href="books/"+book.id)= book.title
          td= book.author
          td.descr= book.description
