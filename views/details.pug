extends layout

block content

  ul
      li.details= book.title
      li.details= book.author
      li.details
        img(src=book.image_url)
      li.details= book.description

      form(name='delete', id='delete-form', action='/books/delete/${book.id}, method='get')
        input(type='submit' value='Delete Book')
